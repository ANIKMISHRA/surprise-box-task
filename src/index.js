import React from 'react';
import ReactDOM from 'react-dom/client';
import SurpriseBox from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SurpriseBox />
  </React.StrictMode>
);
